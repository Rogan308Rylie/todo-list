<!DOCTYPE html>
<html>
<head>
    <title>Todo list</title>
    <link rel="stylesheet" href="css/styles.css">
    <style>
        .clear-form {
            text-align: center;
            margin-top: 20px;
        }
        .clear-btn {
            background-color: #f5f5dc;
            color: black;
            border: none;
            padding: 10px 30px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s;
            white-space: nowrap;
            min-width: 140px;
        }
        .clear-btn:hover {
            background-color: #f0f0d0;
        }
    </style>
</head>
<body>
    <div class="box">
        <h1>Todo List</h1>
    </div>
    <div class="box">
        <% for(var i = 0; i < ejes.length; i++){%>
        <div class="item">
            <input type="checkbox">
            <p><%=ejes[i] %></p>
        </div>
        <% }%>
        <form action="/" method="POST">
            <input type="text" name="ele1" placeholder="Enter the task">
            <button type="submit">+</button>
        </form>
        <form action="/clear?_method=DELETE" method="POST" class="clear-form">
            <button type="submit" class="clear-btn">Clear all tasks</button>
        </form>
    </div>
</body>
</html>
